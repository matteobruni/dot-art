{
  "name": "dot-art",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:deploy": "tsc && vite build --base /dot-art",
    "preview": "vite preview",
    "deploy": "pnpm run build:deploy && gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^7.0.0",
    "vite": "^5.3.4",
    "gh-pages": "^6.1.1"
  }
}
